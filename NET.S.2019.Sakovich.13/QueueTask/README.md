# QueueTask (Day 13, Task 1)

## Task description

The task is to create a class which will implement a queue. The class must implement `IEnumerable<T>` interface
"manually", i.e. without resorting to `yield return` statements.
